apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rss-proxy-rewrite
spec:
  replacePathRegex:
    regex: ^/(config.json|index.html|robots.txt|sitemap.xml)$
    replacement: /verticals-data/rss-proxy/${1}


---

apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rss-proxy-root-rewrite
spec:
  replacePathRegex:
    regex: ^/$
    replacement: /verticals-data/rss-proxy/index.html

---

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rss-proxy-route
spec:
  entryPoints:
    - web
  routes:
    # Handle `/`
    - match: Host(`rss-proxy.staging.migor.org`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: feedless-web
          port: 80
      middlewares:
        - name: rss-proxy-root-rewrite

    # Handle vertical configs
    - match: Host(`rss-proxy.staging.migor.org`) && (Path(`/config.json`) || Path(`/index.html`) || Path(`/robots.txt`) || Path(`/sitemap.xml`) || Path(`/index.html`))
      kind: Rule
      services:
        - name: feedless-web
          port: 80
      middlewares:
        - name: rss-proxy-rewrite

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rss-proxy-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.rss-proxy.staging.migor.org
        - rss-proxy.staging.migor.org
      secretName: feedless-org-tls
  rules:
    - host: api.rss-proxy.staging.migor.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: feedless-core
                port:
                  number: 8080
    - host: rss-proxy.staging.migor.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: feedless-web
                port:
                  number: 80
