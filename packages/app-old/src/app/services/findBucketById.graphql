query bucketById($bucketId: String!) {
  bucket(where: { id: $bucketId }) {
    id
    title
    description
    listed
    in_focus
    streamId
    subscriptions(orderBy: { title: asc }) {
      id
      feed {
        id
        status
        broken
        ownerId
      }
    }
  }
}
