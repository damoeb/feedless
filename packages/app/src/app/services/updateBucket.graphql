mutation updateBucket(
  $id: String!
  $title: String!
  $description: String!
  $listed: Boolean!
) {
  updateBucket(
    where: { id: $id }
    data: {
      description: { set: $description }
      listed: { set: $listed }
      title: { set: $title }
    }
  ) {
    id
  }
}
