query bucketById($data: BucketByIdInput!) {
  bucketById(data: $data) {
    bucket {
      id
      name
      description
      streamId
      websiteUrl
      lastUpdatedAt
      createdAt
      importers {
        id
        autoRelease
        createdAt
        feed {
          id
          createdAt
          websiteUrl
          title
          description
          feedUrl
          status
          lastUpdatedAt
          domain
          genericFeed {
            id
          }
        }
      }
    }
  }
}
