service PrerenderService {
  rpc serverSideStreamingGetListStockQuotes(PrerenderTask) returns (stream PrerenderResult) {}
}

message PrerenderTask {
  required string ticker_symbol = 1;
}
message PrerenderResult {
  required double price = 1;
}
