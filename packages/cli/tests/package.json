{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test $(find . -name '*.test.ts' -not -path './node_modules/*')"
  },
  "devDependencies": {
    "@types/express": "^5",
    "@types/node": "^22",
    "express": "^5",
    "tsx": "^4"
  }
}
