# Plans

Plot is the canonical planning system for this repository as of 2026-09-04. New plans are created here with `/plot-idea <slug>: <title>`; `docs/plans/active/` and `docs/plans/delivered/` index them by phase.

## Frozen, not canonical

These predate Plot and are kept as history. They are not moved, rewritten, or deleted — read them for context, but plan new work through Plot.

- `docs/superpowers/plans/` — six plans written via the superpowers `writing-plans` workflow (2026-07-07 through 2026-07-22).
- `.omc/` — oh-my-claudecode project memory, session state, and notes. Now git-ignored, so it is local-only tooling state rather than shared history.
